{{- if .Values.perfAdvisor.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-perf-advisor-metrics-export-topology
  labels:
    app: {{ .Release.Name }}-perf-advisor
    chart: {{ template "yugaware.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service | quote }}
{{- if .Values.commonLabels }}
{{ include "yugaware.commonLabels" . | indent 4 }}
{{- end }}
data:
  metrics-export.yml: |-
      labels:
        collector_id: local
      shards:
        - id: 0
          default: true
          type: remotewrite
          replicas:
            - endpoint: "http://{{ .Release.Name }}-yugaware-ui:9090/api/v1/write"
              auth:
                type: none
{{- end }}
